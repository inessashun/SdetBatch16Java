package class16;

import org.w3c.dom.ls.LSOutput;

public class PersonTester {
    Person person=new Person();
    //person.password="sfwgargaerg";
    //person.printPassword(); //error because password is private
    //Person.printBalance();
    //System.out.println(person.SSN); //error to access because it is default



}
